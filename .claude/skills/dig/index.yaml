slug: dig
name: "Depth Mode"
version: 1.0.0

description: |
  Use this skill IF user invokes `/dig` OR wants interactive, iterative
  pair-research on a single thread. Resonance-guided depth exploration.

triggers:
  - "/dig"
  - "go deep on"
  - "pull this thread"
  - "k-hole"
  - "dig into"

capabilities:
  model_tier: sonnet
  danger_level: safe
  effort_hint: medium
  downgrade_allowed: true
  execution_hint: sequential
  requires:
    tool_calling: true
    native_runtime: false
    thinking_traces: false
    vision: false
